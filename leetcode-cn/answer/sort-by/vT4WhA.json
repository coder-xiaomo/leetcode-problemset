{
    "data": {
        "solutionArticle": {
            "ipRegion": "未知归属地",
            "rewardEnabled": null,
            "canEditReward": false,
            "uuid": "vT4WhA",
            "title": "排序方式",
            "content": "[TOC]\n\n---\n# 解决方案\n\n## 概述\n\n在这个问题中，你被要求创建一个名为 `sortBy` 的 JavaScript 函数，该函数将根据提供的函数 `fn` 对数组 `arr` 进行排序。函数 `fn` 始终返回一个数字，而这个数字用于确定数组 `arr` 的排序顺序。结果应该是一个按照函数 `fn` 的输出升序排序的数组 `sortedArr`。\n\n问题描述提供了三个关键示例，以阐明预期功能。第一个示例传递了一个数字数组和一个简单地返回其输入的函数，结果是一个按升序排列的数组。第二个示例使用了一个对象数组和一个返回每个对象中特定键（在本例中为 \"x\"）的值的函数，输出是一个根据 \"x\" 的值排序的对象数组。第三个示例包含一个数组的数组，其中的函数返回每个数组的第二个元素（索引为 1）。输出数组根据这些第二个元素按升序排序。\n\n要有效解决这个问题，需要对 JavaScript 的 `Array.sort()` 方法、回调函数和数组操作有很好的理解。你应该能够定义和使用回调函数从数组元素中提取用于排序的值。对 JavaScript 如何处理不同数据类型（数字、对象、数组）的排序也有深入的了解将会很有帮助。\n\n为了全面了解 JavaScript 的 `Array.sort()` 函数和 JavaScript 中的排序，建议查看 [Array.prototype.sort()](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Array/sort) 指南。\n\n### 理解 JavaScript 中的 Array.sort()\n\nJavaScript 内置的 `Array.sort()` 方法用于对数组的元素进行排序。需要注意的是，排序操作直接修改（变异）原始数组，而不是创建一个新的已排序数组。这与一些数组方法（如 `Array.map()`）不同，后者基于原始数组创建一个新数组。在 JavaScript 中，修改原始数组或对象的方法被称为“变异”或“原地”操作。\n\n当我们说 `Array.sort()` “返回”排序后的数组时，是什么意思呢？在 `Array.sort()` 对原始数组进行排序后，它返回对同一（现在已排序）数组的引用。这不是在创建一个包含排序元素的新数组，而是一个便利功能，允许方法链接。\n\n默认情况下，`sort()` 方法将元素转换为字符串，并比较它们的 UTF-16 代码单元值的序列，以确定排序顺序。这对字符串起作用，但在对数字或混合数据类型进行排序时可能导致意外结果。\n\n例如，数组 `[10, 2, 20]` 在使用 `Array.sort()` 排序时会被排序为 `[10, 2, 20]`，因为当转换为字符串时，\"10\" 在字典序中排在 \"2\" 前面。这是为什么在处理数字数据时，通常会在 `sort()` 方法中提供一个比较函数的原因之一。\n\n以下是 `Array.sort()` 方法的基本用法：\n\n```javascript\nlet fruits = [\"Banana\", \"Orange\", \"Apple\", \"Mango\"];\nfruits.sort();\nconsole.log(fruits); // [\"Apple\", \"Banana\", \"Mango\", \"Orange\"]\n```\n\n在上面的示例中，`sort()` 方法按字母顺序排列水果名称。请注意，原始 fruits 数组被就地排序，不会创建新数组。\n\n重要的是要注意，`Array.sort()` 方法不能保证稳定排序。排序算法的稳定性是指在排序输出中相等的元素保留其在原始数组中的相对顺序。`Array.sort()` 函数的稳定性取决于 JavaScript 引擎的具体实现。某些 JavaScript 引擎的实现可能提供稳定性，而其他实现可能不提供。\n\n例如，如果你正在按学生成绩对学生数组进行排序，并且两个学生具有相同的分数，稳定排序将保留这两个学生的原始顺序。然而，如果 JavaScript 引擎的 `Array.sort()` 方法不稳定，这个原始顺序可能不会被保留。\n\n因此，当保持排序输出中相等元素的相对顺序很重要时，应考虑使用或实现一个稳定的排序算法，而不是依赖内置的 `Array.sort()` 方法。\n\n### 使用比较函数进行自定义排序\n\n虽然 `Array.sort()` 不使用比较函数可以满足字符串数组的排序需求，但对于数字数组或需要特定排序标准的对象数组，通常情况下无法得到预期的结果。这就是比较函数派上用场的地方。\n\n`Array.sort()` 允许我们传递一个比较函数，以自定义排序机制。比较函数应该是一个接受两个参数并返回一个负数、零或正数的函数：\n\n- 负数：如果比较函数返回一个小于零的值，它将把 `a` 排到 `b` 之前。简单来说，`a` 应该在 `b` 之前。\n- 正数：如果比较函数返回一个大于零的值，它将把 `a` 排到 `b` 之后。也就是说，`a` 应该在 `b` 之后。\n- 零：如果比较函数返回 `0`，它会保持 `a` 和 `b` 相对于彼此的顺序不变，但它们相对于其他元素的顺序将被排序。\n\n以下是如何使用比较函数按升序对数字进行排序的示例：\n\n```javascript\nlet numbers = [40, 1, 5, 200];\nnumbers.sort((a, b) => a - b);\nconsole.log(numbers); // [1, 5, 40, 200]\n```\n\n在上面的示例中，比较函数是 `(a, b) => a - b`。这个函数从 `b` 中减去 `a`。如果 `a` 小于 `b`，则返回一个负值，将 `a` 放在 `b` 之前。如果 `a` 大于 `b`，返回一个正值，将 `a` 放在 `b` 之后。如果 `a` 等于 `b`，返回 0，保持它们相对位置不变。\n\n这是 JavaScript 中的一个强大工具，因为它允许我们轻松排序复杂的数据结构。例如，你可以基于它们的属性对对象数组进行排序，使用区域设置来对字符串进行排序，甚至实现多条件排序。\n\n例如，考虑按年龄降序对对象数组进行排序：\n\n```javascript\nlet people = [\n  { name: \"John\", age: 23 },\n  { name: \"Amy\", age: 17 },\n  { name: \"Zack\", age: 30 },\n];\npeople.sort((a, b) => b.age - a.age);\nconsole.log(people);\n// [\n//   { name: \"Zack\", age: 30 },\n//   { name: \"John\", age: 23 },\n//   { name: \"Amy\", age: 17 }\n// ]\n```\n\n在这种情况下，`b.age - a.age` 将 `people` 数组按年龄降序排序。\n\n### 在 JavaScript 中使用回调函数\n\n回调函数是一种作为参数传递到另一个函数中的函数。这个传递的函数稍后会被调用，或者会响应包含函数内部的某些事件。使用回调函数是 JavaScript 的一个基本概念，因为它是异步性质的一部分，需要一种方法来管理不立即完成的操作（如网络请求或定时器）。\n\n在我们的 `sortBy` 函数中，回调函数 `fn` 被作为一个参数传递。然后，在 `Array.sort()` 方法的比较函数中使用这个 `fn` 函数来确定数组中元素的排序顺序。\n\n让我们来看一个使用回调函数的简单示例：\n\n```javascript\nfunction greet(name, callback) {\n    console.log('Hello ' + name);\n    callback();\n}\n\n// 使用方法\ngreet('John', function() {\n    console.log('The callback was invoked!');\n});\n\n// Hello John\n// The callback was invoked!\n```\n\n在这个示例中，`greet` 函数的第二个参数是一个回调函数。在记录 'Hello John' 消息后，`greet` 函数调用传入的回调函数。这种将函数作为参数传递以便稍后执行的模式在 JavaScript 中非常常见，用于处理事件、处理异步操作等。\n\n### 自定义比较函数与 Array.sort() 结合的实际应用\n\n1. 在 UI 上显示已排序数据\n\n  `Array.sort()` 的一个常见应用是在用户界面上显示已排序的数据。考虑一个产品列表页面，其中的项目可以按名称、价格或日期进行排序。\n\n  ```javascript\n  let products = [\n    { name: 'Apple', price: 1 },\n    { name: 'Banana', price: 0.5 },\n    { name: 'Cherry', price: 2 }\n  ];\n\n  products.sort((a, b) => a.name.localeCompare(b.name));\n  console.log(products);\n  // [{ name: 'Apple', price: 1 }, { name: 'Banana', price: 0.5 }, { name: 'Cherry', price: 2 }]\n  ```\n\n  这个示例演示了如何使用自定义比较器的 `Array.sort()` 方法，按产品名称的字母顺序对产品对象数组进行排序。`localeCompare()` 方法是 JavaScript 中比较字符串的强大工具，它考虑了字符串比较的特定区域规则。例如，在瑞典语中，\"ä\" 被认为是一个单独的字母，位于字母 \"z\" 之后。简单的比较将无法考虑到这一点。\n\n  通过使用 `localeCompare()`，你可以确保你的代码是完全国际化的，可以正确处理多种人类语言。这对于需要列出产品并需要支持国际化的电子商务应用非常重要。\n\n2. 数据分析和洞察\n\n  `Array.sort()` 经常用于数据分析。如果你正在处理数字数据，排序可以是了解数据集的关键第一步，例如查找中位数值或识别分布。\n\n  ```javascript\n  let numbers = [42, 21, 1, 100, 75, 3];\n  numbers.sort((a, b) => a - b);\n  console.log(numbers);\n  // [1, 3, 21, 42, 75, 100]\n  ```\n\n  在这里，`Array.sort()` 用于按升序对数字数组进行排序。\n\n3. 优先处理任务执行\n\n  在需要按特定顺序处理数据的情况下，`Array.sort()` 非常有用。一个示例可以是根据任务的优先级处理任务。\n\n  ```javascript\n  let tasks = [\n    { title: 'Task 1', priority: 'Low' },\n    { title: 'Task 2', priority: 'High' },\n    { title: 'Task 3', priority: 'Medium' }\n  ];\n\n  let priorityOrder = { 'Low': 1, 'Medium': 2, 'High': 3 };\n\n  tasks.sort((a, b) => priorityOrder[a.priority] - priorityOrder[b.priority]);\n  console.log(tasks);\n  // [{ title: 'Task 1', priority: 'Low' }, { title: 'Task 3', priority: 'Medium' }, { title: 'Task 2',   priority: 'High' }]\n  ```\n\n  在这种情况下，`Array.sort()` 用于根据定义的优先级对任务数组进行排序。\n\n4. 根据深层属性对对象进行排序\n\n  ```javascript\n  let arr = [\n    { prop: { deep: 3 } },\n    { prop: { deep: 1 } },\n    { prop: { deep: 2 } }\n  ];\n\n  arr.sort((a, b) => a.prop.deep - b.prop.deep);\n  console.log(arr);\n  // [{ prop: { deep: 1 } }, { prop: { deep: 2 } }, { prop: { deep: 3 } }]\n  ```\n\n  在这个示例中，`Array.sort()` 根据嵌套属性对对象数组进行排序。\n\n---\n\n### 方法 1：使用自定义比较器\n\n#### 概述\n\n目标是创建一个灵活的排序函数，根据用户提供的函数 `fn` 对数组进行排序。这个 `fn` 函数可以包括任何从数组元素中派生可排序值的逻辑。\n\n#### 算法\n\n- 利用 JavaScript 的 `Array.sort()` 方法，允许使用比较函数定义自定义排序顺序。\n- 比较函数使用 `fn` 从元素中提取可排序值，并比较这些值以确定元素的顺序。\n- 在比较函数中，将元素 `b` 的可排序值减去元素 `a` 的可排序值。如果结果为正数，`a` 将被排序到比 `b` 更高的索引处（即 `a` 在 `b` 之后）。如果为负数，`a` 将被排序到比 `b` 更低的索引处（即 `a` 在 `b` 之前）。如果结果为 `0`，`a` 和 `b` 的相对位置保持不变。\n\n#### 实现\n\n**实现 1：基于减法的比较器**\n\n```javascript []\nvar sortBy = function(arr, fn) {\n    return arr.sort((a,b) => fn(a) - fn(b))\n};\n```\n```typescript []\nfunction sortBy(arr: any[], fn: (item: any) => number): any[] {\n    return arr.sort((a, b) => fn(a) - fn(b));\n}\n```\n\n这段代码利用 JavaScript 的 `Array.sort()` 方法，其中包含一个自定义比较器函数。比较器函数通过对 `a` 和 `b`（要比较的元素）调用 `fn` 来确定它们的排序顺序，然后从 `fn(b)` 中减去 `fn(a)` 的结果来设置 `a` 和 `b` 的排序顺序。\n\n**实现 2：基于比较的比较器**\n\n```javascript []\nvar sortBy = function(arr, fn) {\n    function swap(a, b) {\n        return (fn(a) < fn(b)) ? -1 : 1\n    }\n\n    return arr.sort(swap)\n};\n```\n```typescript []\nfunction sortBy(arr: any[], fn: Function): any[] {\n    function swap(a: any, b: any): number {\n        return (fn(a) < fn(b)) ? -1 : 1;\n    }\n\n    return arr.sort(swap);\n}\n```\n\n在第二个实现中，我们定义了一个比较函数，该函数通过比较 `fn` 应用于 `a` 和 `b` 的结果来确定它们的排序顺序。如果 `fn(a)` 小于 `fn(b)`，它返回 -1，表示 `a` 应该在 `b` 之前。反之，如果 `fn(a)` 不小于 `fn(b)`，它返回 1，表示 `a` 应该在 `b` 之后。\n\n当 `fn(a)` 等于 `fn(b)` 时，比较函数仍然返回 1，这表明 `a` 应该在 `b` 之后。这确实改变了 `a` 和 `b` 的相对顺序，但由于 `a` 和 `b` 通过函数 `fn` 处理后是相同的，所以这种顺序变化不会影响结果排序数组的外观。这是因为两个相同的值之间的“交换”操作不会明显改变数组。\n\n#### 复杂度分析\n\n- 时间复杂度：$O(NlogN)$，其中 $N$ 是输入数组的长度。这是因为 `Array.prototype.sort()` 方法在大多数 JavaScript 引擎中，包括 V8（用于 Chrome 和 Node.js）的最坏情况时间复杂度为 $O(NlogN)$。实际时间复杂度还可以受比较函数（`fn`）的复杂性的影响。\n\n- 空间复杂度：$O(N)$，其中 $N$ 是输入数组的长度。但是，需要注意的是，具体的空间复杂度取决于 JavaScript 引擎使用的具体排序算法，这可能会因不同引擎而异。例如，Chrome 的 V8 引擎对于更长的数组使用 TimSort，其空间复杂度为 $O(N)$，而对于较短的数组使用 InsertionSort，空间复杂度为 $O(1)$。为了进行准确的分析，建议检查你正在使用的 JavaScript 引擎的具体细节。\n\n## 面试提示\n\n1. `Array.prototype.sort()` 方法在 JavaScript 中的作用是什么？\n\n   - `Array.prototype.sort()` 是 JavaScript 中的内置方法，用于对数组的元素进行原地排序。默认情况下，它将元素作为字符串进行排序，这可能导致在对数字进行排序时出现意外的结果。然而，你也可以提供自定义比较函数来确定数组应该如何排序。\n\n2. 比较函数如何与 `Array.prototype.sort()` 结合工作？\n\n   - 在 `Array.prototype.sort()` 中，比较函数是一个接受两个参数（通常称为 `a` 和 `b`）的函数，并返回一个值，指示 `a` 和 `b` 应该相对于彼此如何排序。如果函数返回小于 0 的值，`a` 将在 `b` 之前排序。如果返回大于 0 的值，`a` 将在 `b` 之后排序。如果返回 0，`a` 和 `b` 的相对顺序保持不变。\n\n3. 如何基于特定属性对对象数组进行排序？\n\n   - 要基于特定属性对对象数组进行排序，你可以使用 `Array.prototype.sort()` 并提供自定义比较函数。比较函数可以访问两个对象上的所需属性，并根据该属性的值确定它们的排序顺序。\n\n4. `String.prototype.localeCompare()` 做什么，为什么在比较函数中使用它？\n\n   - `String.prototype.localeCompare()` 是一个方法，它根据它们的区域设置（即语言和地区）比较两个字符串。它考虑了特定区域设置的字符串排序规则，因此是进行国际化字符串比较的理想工具。在比较函数中使用它可以确保字符串按照与用户所使用的语言和文化相关的方式进行排序。\n\n5. 在编写具有复杂排序需求的代码时，为什么使用自定义比较函数是一种好思路？\n\n   - 使用自定义比较函数允许你在排序过程中应用复杂的逻辑，而不仅仅是默认的字符串或数字排序。这对于基于多个条件排序、国际化排序或特定数据需求的排序非常有用。自定义比较函数使你能够完全控制元素的排序顺序。\n\n6. 有哪些排序算法可用于实现 `Array.prototype.sort()`？\n\n   - JavaScript 引擎的实现可以选择不同的排序算法。一些 JavaScript 引擎使用快速排序（例如 V8 引擎），而其他引擎可能使用归并排序。对于小型数组，有时还可以使用插入排序。对于稳定性排序，一些引擎可能使用 TimSort。具体的算法取决于引擎的实现。不过，这不会影响使用 `Array.prototype.sort()` 时的代码，因为接口是相同的，只是具体实现可能会有所不同。\n\n7. 为什么稳定性在排序算法中重要？能举一个稳定排序的例子吗？\n\n   - 稳定性在排序算法中重要，因为它确保相等的元素在排序后保持它们在原始数组中的相对顺序。这对于需要保留某些元素相对于其他元素的原始顺序的情况非常有用。例如，如果你正在对学生成绩的数组进行排序，并且有多名学生具有相同的分数，稳定排序算法将确保它们在排序后的数组中保持与原始数组中的相对顺序一致。TimSort 是一个示例，它是一个稳定排序算法，通常用于某些 Java 和 Python 的排序实现。\n\n### 示例代码\n\n```javascript\n// 示例 1: 基于数字数组的排序\nconst numbers = [5, 3, 8, 1, 9];\nconst sortedNumbers = sortBy(numbers, num => num);\nconsole.log(sortedNumbers); // [1, 3, 5, 8, 9]\n\n// 示例 2: 基于对象数组的排序\nconst people = [\n  { name: \"Alice\", age: 30 },\n  { name: \"Bob\", age: 25 },\n  { name: \"Eve\", age: 35 },\n];\nconst sortedPeople = sortBy(people, person => person.age);\nconsole.log(sortedPeople);\n// [\n//   { name: \"Bob\", age: 25 },\n//   { name: \"Alice\", age: 30 },\n//   { name: \"Eve\", age: 35 }\n// ]\n\n// 示例 3: 基于嵌套对象的排序\nconst data = [\n  { info: { value: 10 } },\n  { info: { value: 5 } },\n  { info: { value: 15 } },\n];\nconst sortedData = sortBy(data, item => item.info.value);\nconsole.log(sortedData);\n// [\n//   { info: { value: 5 } },\n//   { info: { value: 10 } },\n//   { info: { value: 15 } }\n// ]\n```\n\n这些示例演示了如何使用 `sortBy` 函数对不同类型的数据进行排序，包括数字数组、对象数组以及具有嵌套属性的对象数组。函数接受一个提取排序键的回调函数，并返回一个按照这些键升序排序的新数组。",
            "slateValue": "",
            "slug": "pai-xu-fang-shi-by-leetcode-solution-1p3m",
            "sunk": false,
            "chargeType": "FREE",
            "status": "PUBLISHED",
            "identifier": "vT4WhA",
            "canEdit": false,
            "canSee": true,
            "reactionType": null,
            "reactionsV2": [
                {
                    "count": 3,
                    "reactionType": "UPVOTE"
                }
            ],
            "tags": [
                {
                    "name": "JavaScript",
                    "nameTranslated": "",
                    "slug": "javascript",
                    "tagType": "LANGUAGE"
                },
                {
                    "name": "TypeScript",
                    "nameTranslated": "",
                    "slug": "typescript",
                    "tagType": "LANGUAGE"
                }
            ],
            "createdAt": "2023-10-31T12:13:45.027995+00:00",
            "thumbnail": "",
            "author": {
                "username": "LeetCode-Solution",
                "certificationLevel": "OFFICIAL",
                "isDiscussAdmin": true,
                "isDiscussStaff": true,
                "profile": {
                    "userAvatar": "https://assets.leetcode.cn/aliyun-lc-upload/users/leetcode-solution/avatar_1582018938.png",
                    "userSlug": "leetcode-solution",
                    "realName": "力扣官方题解",
                    "reputation": 245
                }
            },
            "summary": "解决方案 概述 在这个问题中，你被要求创建一个名为 sortBy 的 JavaScript 函数，该函数将根据提供的函数 fn 对数组 arr 进行排序。函数 fn 始终返回一个数字，而这个数字用于确定数组 arr 的排序顺序。结果应该是一个按照函数 fn 的输出升序排序的数组 sortedArr。 问题描述提供了三个",
            "topic": {
                "id": 2506901,
                "subscribed": false,
                "commentCount": 6,
                "viewCount": 254,
                "post": {
                    "id": 4670260,
                    "status": "Prepub",
                    "voteStatus": 0,
                    "isOwnPost": false
                }
            },
            "byLeetcode": true,
            "isMyFavorite": false,
            "isMostPopular": false,
            "favoriteCount": 9,
            "isEditorsPick": false,
            "hitCount": 6323,
            "videosInfo": [],
            "question": {
                "titleSlug": "sort-by",
                "questionFrontendId": "2724"
            },
            "next": {
                "slug": "sort-by-adela2012-qdxt",
                "title": "sort排序"
            },
            "prev": null
        }
    }
}